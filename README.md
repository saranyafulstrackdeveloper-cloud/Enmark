
  # enmark

  This is a code bundle for enmark. The original project is available at https://www.figma.com/design/58iF55QUkbSLQ564ZgNfE3/enmark.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  